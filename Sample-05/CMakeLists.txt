set(ProjectName Sample-05)
project(${ProjectName})
set(Project_SRC Sample.cpp)
add_executable(${ProjectName} ${Project_SRC})
if(WIN32)
	target_link_libraries(${ProjectName} DUOlib) 
elseif(UNIX OR APPLE)
	target_link_libraries(${ProjectName} duo) 
endif()